machine:
  java:
    version: openjdk7
dependencies:
  override:
    - mvn --fail-never dependency:go-offline || true
  pre:
    - wget https://dl.bintray.com/jfrog/jfrog-cli-go/1.7.1/jfrog-cli-linux-amd64/jfrog
    - chmod +x jfrog
compile:
  override:
    - mvn clean install
    - ./jfrog rt config --url https://gcartifactory-us.jfrog.info/artifactory --user jainishs --apikey AKCp2UNgyicsv5cHr3BvMAaaWd9d123jvpTudVKPE6KWghJLRXUgvxjmsEjgE8svEFksAkRFi
    - ./jfrog rt u "*" circleci-generic-local
